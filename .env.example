# Backend API origin (no trailing path — /api is appended in production)
# Dev: Vite proxy forwards /api → this URL
# Prod (Vercel): set the same key in Project → Settings → Environment Variables, then redeploy
VITE_API_BASE_URL=http://127.0.0.1:3000
